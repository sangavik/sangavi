#include<stdio.h>
int main()
{
int a,b,c;
printf("enter three nums\n");
scanf("%d%d%d",&a,&b,&c);
if((a>b)&&(a>c))
{
printf("\n %d is larger than %d",&a7b&c);
}
if((b>a)&&(b>c))
{
printf("\n %d is larger than %d",&b&a&c);
}
if((c.a>&&(c>b))
{
printf("\n %d is larger than %d",&c&b&a);
}
return 0;
}
